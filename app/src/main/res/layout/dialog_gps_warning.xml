<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/app_medium_spacing"
        android:orientation="vertical">

        <TextView
            android:id="@+id/lblTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="@dimen/app_large_spacing"
            android:paddingEnd="@dimen/app_large_spacing"
            android:padding="@dimen/app_medium_spacing"
            android:text="@string/dialog_gps_warning_title"
            android:textAppearance="@style/TextAppearance.MaterialComponents.Headline6"
            android:textStyle="bold" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="@dimen/app_large_spacing"
            android:paddingEnd="@dimen/app_large_spacing"
            android:paddingBottom="@dimen/app_large_spacing"
            android:padding="@dimen/app_medium_spacing"
            android:text="@string/dialog_gps_warning_message"
            android:textAppearance="@style/TextAppearance.MaterialComponents.Body1" />

    </LinearLayout>

</layout>
